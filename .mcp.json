{
  "mcpServers": {
    "siftmemory-memory": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/mcp/server.js"],
      "env": {
        "SIFTMEMORY_DAEMON_URL": "${SIFTMEMORY_DAEMON_URL}"
      }
    }
  }
}
